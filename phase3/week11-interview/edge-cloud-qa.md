# 端云协同与端侧 AI 面试题

结合 `week3/`、`week4/`、`projects/direction-a-smart-notes/`。

---

## 端云协同

### 1. 什么是端云协同？

**答**：按任务复杂度、隐私、成本将请求分配到端侧小模型或云端大模型，必要时走 Agent 工具链。

**项目**：`week4/router.py` — `local` / `cloud` / `agent` 三路路由。

---

### 2. 什么场景走端侧？什么走云端？

| 端侧 local | 云端 cloud | Agent |
|------------|------------|-------|
| 短问候、简单确认 | 长文本分析、复杂推理 | 需工具：天气、计算、知识库 |
| 弱网兜底 | 高质量生成 | 多步工具协作 |

**项目**：`week4/settings.py` 关键词规则；智能笔记 `chat_service.py` 叠加笔记 RAG。

---

### 3. 路由规则可以怎么优化？

**答**：当前是规则路由（关键词 + 长度），可演进为：
- 轻量分类模型
- 用户可选手动「深度思考」
- 记录 route 日志做分析与 A/B

**诚实说明**：学习项目用规则即可解释、可调试。

---

### 4. 端云协同的隐私优势？

**答**：敏感输入可在端侧预处理或本地小模型处理；正文检索可在本地向量库（笔记 App）；仅复杂问题才上云。

---

## 端侧 AI

### 5. 端侧部署的优势与局限？

| 优势 | 局限 |
|------|------|
| 低延迟、弱网可用 | 模型能力弱于云端 |
| 隐私好 | 包体大、耗电 |
| 省 API 成本 | 真机集成复杂（MLC/LiteRT） |

**项目**：`week3/local_llm.py` — Mock 与 Qwen2.5-0.5B。

---

### 6. OnDeviceLLM 抽象为什么重要？

**答**：隔离推理实现，UI 层不感知 Mock 还是真实 MLC 模型，便于渐进式集成。

**项目**：`week3/android-app/.../OnDeviceLLM.kt`。

---

### 7. 真机如何接入 Qwen2.5？

**答**（概览）：MLC LLM 或 LiteRT-LM 量化模型 → 放入 assets → 实现 `OnDeviceLLM` → 替换 `MockOnDeviceLLM`。详见 `week3/README.md` Step 5。

---

## 综合追问

| 问题 | 回答要点 |
|------|----------|
| 智能笔记为何保存时索引？ | 保证问答与笔记一致 |
| 端侧 Mock 有何价值？ | 开发/演示/CI 不依赖 GPU |
| 如何观测路由决策？ | 返回 `route` + `reason`（Direction A API） |

---

## 自测题

1. 对「你好」和「分析微服务架构步骤」分别会走哪条路由？
2. 画端云协同三层：客户端 / 路由 / 后端能力。
